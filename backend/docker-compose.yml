version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - ./logs:/app/logs
    environment:
      - NODE_ENV=development
    restart: unless-stopped
    container_name: tnt-backend-mock
